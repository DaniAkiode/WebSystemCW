const router = require('express').Router();
const { rawListeners } = require('../models/profile.model');
let Exercise = require('../models/profile.model');

router.route('/').get((req, res) => {
    
})